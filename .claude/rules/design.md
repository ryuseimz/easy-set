# Design Rules

- Start from user flow and state model, not pixels alone.
- Use Figma MCP when a design source exists.
- Prefer existing design tokens and components.
- If building from screenshot, compare final UI with screenshot and list visible differences.
- Accessibility is part of completion: labels, keyboard focus, contrast, reduced motion where relevant.

