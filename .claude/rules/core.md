# Core Rules

- Prefer small, reversible changes.
- Use existing project patterns before introducing new abstractions.
- Add tests when changing behavior.
- Run the narrowest meaningful verification first, then broader checks before shipping.
- Do not hide failures by weakening tests, lint rules, or type checks.

